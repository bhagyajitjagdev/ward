# openapi-typescript declares peer typescript@^5.x but works fine on the TS 6 this
# project uses (codegen only — it never runs in the app). Until its peer range
# catches up, accept the mismatch everywhere npm runs (dev, CI, docker's npm ci).
legacy-peer-deps=true
