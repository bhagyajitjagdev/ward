# Go build artifacts
/backend/ward
/backend/**/*.test
*.out

# Local databases
*.db
*.db-wal
*.db-shm

# Local caches / scratch
.cache/

# Local TLS certs/keys (self-signed test material — may carry a real test hostname; never commit)
*.pem
*.key
*.crt

# Frontend (ward-ui, later)
node_modules/
dist/

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/
